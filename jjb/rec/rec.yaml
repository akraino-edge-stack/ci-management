---
- project:
    name: 'rec'
    project-name: 'rec'
    project: '{project-name}'
    views:
      - project-view
    # -------------------------------
    # BRANCH DEFINITIONS
    # -------------------------------
    stream:
      - master:
          branch: '{stream}'
          gs-pathname: ''
          disabled: false

    # -------------------------------
    # POD DEFINITIONS
    # -------------------------------

    slave-label: 'aarch64_baremetal'

    jobs:
       - akraino-rec-deploy
       - akraino-rec-install-rec
       #- akraino-rec-install-ric (TBD)
